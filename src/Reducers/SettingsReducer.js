// import {} from './../Actions/types';

const INITIAL_STATE = {
    showProgress: true,
    advancedStandings: false
}

export default (state=INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
